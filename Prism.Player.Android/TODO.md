# TODO — Prism.Player.Android

Список отложенных задач. Порядок и приоритеты — по решению пользователя.

## Важные мелочи — ВСЁ СДЕЛАНО
- **Тёмная плоская тема** (`AppTheme` = Material dark, без action bar, fullscreen;
  фон `#282828`) — против выгорания экрана ТВ.
- **Видео на весь экран** при открытии (`PlayerView` `MATCH_PARENT`, без верхней панели);
  фон под видео чёрный — поля по краям кадра при несовпадении пропорций не должны
  светиться серым фоном приложения (проверено на приставке: 0,0,0).
- **Пауза при сворачивании** (HOME/фон → `MainActivity.onStop` → `controller.pause()`).
- **Закрытие фильма с пульта:** двойной Back → меню (первый Back — тост; окно 3 с);
  авто-закрытие при 100% (`STATE_ENDED`).
- **Логотип на главном экране** (`main_screen.png` по центру).
- **Кнопка «Настройки» — шестерёнка** (`ImageButton`, стандартная кнопка) в правом
  верхнем углу.

Фирменные ассеты (из `assets/`): иконка `mipmap/ic_launcher` (`app-icon.png`, 512),
ТВ-баннер `drawable-xhdpi/banner.png` (320×180), логотип на главном
`drawable-nodpi/main_screen.png` (900×260). Фон `#282828`.

## 1. Полностью реализовать MQTT-протокол
Плеер принимает `open` / `close` / `refresh`. `open` несёт **абсолютный `url`**
(что играть) — плеер host-agnostic.

**Отправка статуса — СДЕЛАНО (2026-07-08):** retained `info` (`name`, `capabilities`;
без `prismUrl`) при коннекте/по `refresh`; retained `state` (`status`, `url?`,
`positionSec`, `durationSec?`) при смене + периодически ~5 с (heartbeat).
`availability`/LWT НЕ делаем — ливность по свежести `state`; команда `refresh` —
немедленная перепубликация. `event` и `title` в `state` — отложены.

Осталось — команды (**пока НЕ делаем** — нет уверенности, что нужны; корневой
`TODO.md`, п.10):
- `pause`, `resume`, `stop`, `seek`, `next`, `previous`, `setAudio`, `setSubtitle`,
  `setRate`;
  - `next`/`previous` считает координатор (плеер не знает библиотеку) и шлёт URL;
  - `setAudio`/`setSubtitle`: мультитрек-HLS в Prism готов (корневой `TODO.md`,
    п.3 сделан 2026-08-10) — ExoPlayer уже видит дорожки из одного URL, осталось
    только замапить команды на `TrackSelectionParameters`.

Контракт топиков/пейлоадов — в `README.md` (раздел «MQTT-контракт управления»).

## 2. Экран настроек — ЧАСТИЧНО СДЕЛАНО (2026-07-06)
Сделано: экран настроек (кнопка «Настройки» с главного экрана) + хранение в
`SharedPreferences` (`Settings.kt`). Поля: адрес брокера MQTT (+ логин/пароль),
отображаемое имя (URL Prism убран — плеер host-agnostic). Идентификатор плеера генерируется автоматически
(`UUID`; в debug — `emulator`) и **не редактируется**. При пустых/битых настройках
плеер не подключается/не играет, но не падает (валидацию ввода на экране не делаем).
Применение — перезапуск `MqttService` при сохранении. Константы адресов вынесены из
`MqttController`/`MqttService` в `Settings.kt`.

Осталось:
- **автозаполнение настроек** (**в самом конце** — удобство, не необходимость;
  корневой `TODO.md`, п.9): получать предзаполненный набор настроек, чтобы
  пользователю не вводить всё руками. Продумать удобный способ (варианты:
  QR-код / ссылка-приглашение / discovery в сети / дефолты от координатора).
  Требует отдельного обсуждения.

## 3. Фон / автозапуск по загрузке — СДЕЛАНО (2026-07-03)
Две службы (чтобы не конфликтовали за «передний план»):
- **`MqttService`** — всегда-живая служба переднего плана: MQTT + «пульт»
  (`MediaController`) к плееру; своё постоянное уведомление (не пропадает),
  `START_STICKY`, `onTaskRemoved` — живёт после закрытия приложения;
- **`PlaybackService`** (`MediaSessionService`) — плеер + сессия; Media3 сама рулит
  медиа-уведомлением;
- `BootReceiver` стартует `MqttService` по `BOOT_COMPLETED`;
- активация экрана из фона по `open` через `startActivity` + разрешение
  `SYSTEM_ALERT_WINDOW` (пользователь выдаёт один раз). См. README, раздел
  «Фон, автозапуск и активация». Два уведомления при игре — так задумано.

## 5. Запоминание позиции просмотра и «продолжить с места» — СДЕЛАНО (2026-08-11)
`PlaybackPositions` — локальная память в `SharedPreferences`: `url → «мс|когда»`,
не больше 100 записей (старые вытесняются), только на устройстве. Сохранение —
попутно с публикацией `state` (тикер 5 с + события пауз) и при `close`.
**Автопродолжение**: `open` с сохранённой позицией стартует с неё
(`setMediaItem(item, startMs)`) + тост «Продолжаем с 1:23:45» (RU/EN); диалог с
выбором решили не делать. **Сброс**: `STATE_ENDED` или ≥95% — запись удаляется,
позиции короче 10 с не хранятся. Проверено на приставке: резюм после закрытия
(тост на экране), после досмотра до конца — запуск с нуля. Попутно включена
кнопка субтитров на панели плеера (`setShowSubtitleButton(true)`).

## 4. Упаковка для Android TV + релизная сборка — СДЕЛАНО (2026-07-08)
- **TV-лаунчер:** в `MainActivity` добавлена `LEANBACK_LAUNCHER`; в манифесте
  `uses-feature` leanback + touchscreen (`required=false`); иконка `@mipmap/ic_launcher`
  (из `app-icon.png`) и баннер `@drawable/banner`. Приложение появляется на домашнем
  экране Android TV.
- **Release-подпись:** `signingConfigs.release` в `app/build.gradle.kts` читает
  `keystore.properties` (не в git) + keystore `prism-release.jks`; `assembleRelease`
  даёт подписанный `app-release.apk` с UUID-id и пустыми настройками. Проверено на
  эмуляторе: ставится, `id`=UUID, подпись `CN=Prism Player`.
