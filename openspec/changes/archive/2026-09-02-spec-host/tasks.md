## 1. Сверка `host/media-catalog` с поведением

- [x] 1.1 Проверить требование «Сканирование папок с медиа» по коду
      (`PrismHostApp.ApplyCommandLine`, `MediaLibrary`) — сценарии нескольких
      папок из конфига и командной строки.
- [x] 1.2 Проверить требование «Сведения о сервере» — состав ответа
      `GET /api/info` совпадает с `PrismHostApp.cs`.
- [x] 1.3 Проверить требование «Список файлов каталога не блокируется разбором
      метаданных» — состояние `pending` и его поля.
- [x] 1.4 Проверить требование «Карточка одного файла дожидается полного
      разбора» — синхронный резолв в `GET /api/media/{id}`.
- [x] 1.5 Проверить требование «Относительный путь файла в карточке» —
      `relativePath` заполняется сканом.

## 2. Сверка `host/streaming` с поведением

- [x] 2.1 Проверить требование «Выбор режима воспроизведения» — правила
      `direct`/`hls`/`unsupported` в `Media/MediaInfo.cs`/`MediaLibrary.cs`.
- [x] 2.2 Проверить требование «Прямая раздача с поддержкой Range» —
      `GET /raw/{id}`.
- [x] 2.3 Проверить требование «HLS master-плейлист с рендициями дорожек» —
      `HlsTranscoder.BuildMasterPlaylist`.
- [x] 2.4 Проверить требование «VOD-плейлисты рассчитаны заранее по полной
      длительности».
- [x] 2.5 Проверить требование «Сегменты производятся сессиями
      транскодирования с ограниченным диапазоном» — `TranscodeSession`,
      вытеснение по LRU, уборка простаивающих сессий.
- [x] 2.6 Проверить требование «Извлечение и доставка субтитров» —
      `SubtitleService`, `textBased`.
- [x] 2.7 Проверить требование «Дорожки из соседних файлов» —
      `Player:SubtitleFiles`/`Player:AudioFiles`, `external: true`.

## 3. Сверка `host/content-identity` с поведением

- [x] 3.1 Проверить требование «Контентный идентификатор файла» — вычисление и
      персистентный кэш отпечатков.
- [x] 3.2 Проверить требование «Резолв файла по отпечатку» — `GET /api/resolve`.
- [x] 3.3 Проверить требование «Поиск преемника при изменении содержимого по
      прежнему пути» — `GET /api/media/{id}/successor`.

## 4. Завершение

- [x] 4.1 Прогнать `openspec validate spec-host --strict` и исправить замечания.
