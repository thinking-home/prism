## 1. Сверка `library/catalog-aggregation` с поведением

- [x] 1.1 Проверить требование «Конфигурация хостов» — `HostCatalog.Normalize`.
- [x] 1.2 Проверить требование «Слияние каталога всех хостов» —
      `HostCatalog.GetMergedAsync`, порядок приоритета, `Absolutize`.
- [x] 1.3 Проверить требование «Недоступный хост не ломает выдачу каталога» —
      таймаут `HostCatalog.HostTimeout`, обработка исключений опроса.
- [x] 1.4 Проверить требование «Адресный доступ к карточке одного файла» —
      `HostCatalog.GetItemAsync`, `FileHostLedger.HostsForAsync`.
- [x] 1.5 Проверить требование «Мета из БД подмешивается поверх данных хоста» —
      `LibraryEndpoints` (`/api/media`, `/api/media/{id}`).

## 2. Сверка `library/tree-and-metadata` с поведением

- [x] 2.1 Проверить требование «Дерево групп с вложенностью» —
      `GET /api/library/tree`.
- [x] 2.2 Проверить требование «CRUD групп» — создание/перенос/удаление,
      защита от циклов.
- [x] 2.3 Проверить требование «Членство файла в группе» — PUT/DELETE items.
- [x] 2.4 Проверить требование «Свободная мета файлов и групп» —
      `MergeMetaAsync`, слияние и удаление по `null`.

## 3. Сверка `library/auto-rules` с поведением

- [x] 3.1 Проверить требование «Правило — шаблон пути с плейсхолдерами» —
      `LibraryRules.cs` (`Compile`, `Match`).
- [x] 3.2 Проверить требование «Правила применяются только к нетронутым
      файлам» — `LibraryMaintenanceService.ApplyRulesAsync` (`touched`).
- [x] 3.3 Проверить требование «Порядок применения правил и приоритет
      мета-ключей».
- [x] 3.4 Проверить требование «Полная переразметка библиотеки по запросу» —
      `_replaceRequested`, `ClearLibraryAsync`, порядок относительно прохода.

## 4. Сверка `library/fingerprint-remap` с поведением

- [x] 4.1 Проверить требование «Обнаружение осиротевших записей» —
      `LibraryMaintenanceService.RemapAsync`.
- [x] 4.2 Проверить требование «Перенос записей на преемника» — случаи
      свободного и занятого преемника.
- [x] 4.3 Проверить требование «Ремап выполняется раньше автозаполнения» —
      порядок вызовов в `ExecuteAsync`.
- [x] 4.4 Проверить требование «Адресный поиск преемника через бухгалтерию
      "id → хост"» — `HttpMediaIdentity`, `FileHostLedger`.
- [x] 4.5 Проверить требование «Сборка мусора по явному запросу» —
      `POST /api/library/gc`.

## 5. Сверка `library/players` с поведением

- [x] 5.1 Проверить требование «Обнаружение плееров по MQTT» —
      `PlayerRegistry.OnMessage`, окно свежести.
- [x] 5.2 Проверить требование «Список плееров» — `GET /api/players`, 503 без
      брокера.
- [x] 5.3 Проверить требование «Включение файла на плеере» —
      `POST /api/players/{id}/open`, все коды ошибок.

## 6. Сверка `library/web-client-hosting` с поведением

- [x] 6.1 Проверить требование «Раздача собранного веб-клиента» —
      `PrismLibraryApp.ResolveClientPath`, поведение при отсутствии папки.
- [x] 6.2 Проверить требование «Веб-клиент по умолчанию обращается к
      same-origin библиотеке» — настройка адреса в `Prism.Client`.

## 7. Завершение

- [x] 7.1 Прогнать `openspec validate spec-library --strict` и исправить
      замечания.
