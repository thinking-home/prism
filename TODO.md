# TODO — Prism (общий список)

Отложенные задачи уровня системы. Порядок и приоритеты — по решению пользователя.

## 1. Структурирование библиотеки
- хранить **виртуальное дерево** библиотеки (папки/категории/разделы) и его
  **маппинг на физические файлы** — виртуальная структура не привязана к
  расположению файлов на диске;
- хранить **мета-информацию** и уметь **группировать/фильтровать** по ней.

Развивает плагин метаданных (`Prism.Plugins.Library`): сейчас метаданные плоские
(фильм → title; эпизод → сериал/сезон/эпизод). Нужно перейти к дереву + фильтрам.

## 2. Умный дом: автообнаружение плееров и запуск кино на выбранном
- в умном доме (ThinkingHome) — **автообнаружение всех плееров Prism по MQTT**
  (подписка на `prism/player/+/info` и `prism/player/+/availability` из контракта);
- из библиотеки — **включать кино на заданном плеере**: резолв «название → mediaId»
  через API Prism и публикация команды `open` в топик выбранного плеера.

Это сторона координатора (плагин ThinkingHome). Зависит от отправки статуса плеером
по MQTT (см. `Prism.Player.Android/TODO.md`, п.1).

---
Отдельный TODO по Android-плееру — в `Prism.Player.Android/TODO.md`.
