# TODO — Prism (общий список)

Задачи в порядке приоритета (зафиксировано 2026-08-10).

## 1. Логи хоста в файлы (с ротацией) — СДЕЛАНО (2026-08-10)
Serilog в `PrismHostApp` (общая точка консоли и службы): консоль + файл
`logs/prism-<дата>.log` рядом с приложением, ротация по дню и размеру,
ограничение числа файлов. Уровни — секция `Serilog` в `appsettings.json`.

## 2. Множественные папки с видео на хосте — СДЕЛАНО (2026-08-10)
Конфиг: `Player:MediaDirectories` — список вместо строки (ключ `MediaDirectory`
удалён, совместимость не держим — проект не опубликован). `--media`/`--file`
можно указывать несколько раз; папки из командной строки **добавляются** к списку
из конфига — у службы Windows `--media` от инсталлятора работает вместе с
папками, дописанными в `appsettings.json`. `/api/info` отдаёт массив
`mediaDirectories`. Дубли папок и вложенные корни схлопываются, недоступная папка
не ломает остальные.

## 3. Мультитрек HLS (master-плейлист) — СДЕЛАНО (2026-08-10)
`playlist.m3u8` теперь **master-плейлист** с рендициями `#EXT-X-MEDIA:TYPE=AUDIO`
(каждая аудиодорожка — отдельный сегментный поток, лёгкая ffmpeg-сессия
только-аудио) и `TYPE=SUBTITLES` (обёртка над WebVTT). Плееры (ExoPlayer, hls.js,
Safari) переключают дорожки локально, из одного URL, без координатора; смена аудио
больше не перекодирует видео. Старый муксованный режим `?audio=N` удалён;
веб-клиент переведён на `hls.audioTrack`. URL для Android-плеера не изменился.

Осталось на потом: графические субтитры (PGS/VOBSUB) — только прожигом
(отдельная задача, если понадобится).

**Проверено на приставке (2026-08-11):** меню аудиодорожек ExoPlayer показывает,
переключение работает (проверено на тестовом файле с двумя тонами). Кнопка
субтитров появилась после `setShowSubtitleButton(true)` в плеере. **ПРОБЛЕМА:
субтитры на приставке не отрисовываются** (выбор дорожки есть, текста нет; в
hls.js работает). Вероятная причина: наш субтитровый плейлист — один
WebVTT-«сегмент» на весь фильм без `X-TIMESTAMP-MAP`, ExoPlayer к этому строже.
Разобраться отдельно: сегментировать VTT по сетке / добавить X-TIMESTAMP-MAP.

## 4. Инсталлятор Windows (`Prism.Host.Setup`) — ЗАБЛОКИРОВАНО
Нужно делать на компьютере с Windows.

- **убрать галочку подтверждения лицензии**: шаг с текстом (MIT + сведения о
  вложенном FFmpeg) оставить информационным, но не требовать «I accept» для
  кнопки Next;
- **починить remember-паттерн**: при повторной установке путь к папке с видео
  не подхватывается из реестра (`HKLM\SOFTWARE\Prism\Host\MediaFolder`) — в
  диалоге снова показывается дефолт `C:\Videos` вместо сохранённого значения.
  Разобраться с порядком `AppSearch`/`SetMEDIAFOLDER`/costing (вероятно,
  set-свойства недостаточно, чтобы переопределить значение Directory до
  `CostFinalize`, или мешает значение по умолчанию из таблицы Directory).
- **enable/disable полей MQTT** в диалоге `PrismConfigDlg`: сейчас поля MQTT
  всегда активны (значения просто игнорируются, если галочка «установить
  лаунчер» снята). Хотелось бы серать их при снятой галочке — в WiX 6 дочерний
  `<Condition Action="enable|disable">` у `<Control>` не принимается (WIX0005),
  нужно найти актуальный синтаксис ControlCondition для v6.
- перенести версию пакета из хардкода в `Package.wxs` в свойство сборки.
- **несколько папок с видео**: хост уже умеет (п.2), но диалог инсталлятора
  выбирает одну. Не критично: дополнительные папки дописываются в
  `appsettings.json` службы (CLI добавляется к конфигу, а не заменяет его).
- **проверка .NET 10 Runtime в инсталляторе**: программы теперь
  framework-dependent, нужен рантайм (ASP.NET Core — для хоста, Desktop — для
  лаунчера). Надёжной декларативной проверки нет: ключи реестра
  `HKLM\SOFTWARE\dotnet\Setup\InstalledVersions\...\sharedfx\*` отсутствуют, если
  рантайм пришёл с SDK (проверено — заблокировало бы саму дев-машину), а
  файловый `DirectorySearch` упирается в то, что `[ProgramFiles64Folder]` ещё не
  разрешён на этапе `AppSearch`. Правильное решение — **bootstrapper WiX Burn**,
  который сам ставит нужные рантаймы перед MSI (отдельный Bundle-проект). Пока
  полагаемся на встроенный диалог apphost'а («установите .NET…») + README.

## 5. Запоминание позиции просмотра — СДЕЛАНО (2026-08-11)
Реализовано в Android-плеере и проверено на приставке: локальная память
url → позиция, автопродолжение с тостом, сброс при досмотре. Детали — в
`Prism.Player.Android/TODO.md`, п.5.

## 6. Структурирование библиотеки — СПЕКА СОГЛАСОВАНА (2026-08-11), ждёт реализации

Развитие `Prism.Plugins.Library`; ядро (скан/стриминг) не трогаем.

**Модель — две порции данных:**
- *скан* — эфемерный слепок диска (path → fingerprint); отпечатки кэшируются в БД
  по `(path, size, mtime)`, чтобы не пересчитывать после рестарта;
- *библиотека* — персистентная: дерево групп + мета + членство. Путей не знает,
  ключ файла — **fingerprint** (структура переживает переезды/переименования).
  Копии одного контента в разных папках разделяют мету — это осознанно.

**Таблицы:** `Prism_Node(Id, ParentId, Name)` — группы, вложенность через ParentId;
`Prism_NodeItem(NodeId, FileKey)` — членство (файл может быть в нескольких группах);
`Prism_Meta(EntityType, EntityKey, Key, Value)` — свободные ключи и для файлов, и
для групп. Сервер семантики меты НЕ знает (никаких movie/episode; `title` хранится
готовой строкой, склейку делает заполняющий). Старая таблица `Prism_MediaMetadata`
мигрируется в key-value; `PUT /api/media/{id}/metadata` заменяется на общий
`PUT …/meta` (слияние ключей, null — удалить).

**API:** `/api/media` как сейчас (мета подмешивается); `GET /api/library/tree` —
все группы + членство одним запросом (библиотека домашняя — без пагинации, дерево
строит клиент); CRUD групп (перенос ветки = смена ParentId); членство
PUT/DELETE. **Поиск — только на клиенте** (фильтрация полного списка в памяти).

**Неконсистентность — штатное состояние, не ошибка:**
- файл на диске без записей — «новый»: виден в корне, мета пустая;
- записи без файла — «осиротевшие»: автоматически НЕ удаляем (диск мог быть
  отключён), в API помечаются `present:false`; чистка — явной командой (GC).

**Правила автозаполнения** — атомарные, декларативные, применяются ОДИН раз при
первом появлении файла (обработанные fingerprint помечаются; ручные правки правила
не перетирают):
- правило = regex по относительному пути + действия `group` (шаблон пути групп,
  создаются при необходимости) и/или `meta` (ключи-шаблоны из захваченных групп),
  напр. `serials/(?<series>[^/]+)/S(?<s>\d+)E(?<e>\d+)` →
  группа `Сериалы/{series}/Сезон {s}`, мета `title={series} · S{s}E{e}`;
- применяются все подходящие по порядку; хранение — конфиг/json (UI/API позже);
- всё умное (TMDB и т.п.) — снаружи через API.

**Недокачанные/изменившиеся файлы — примитивный ремап при скане:**
fingerprint F осиротел → взять его последний путь P из кэша → если в P лежит
файл с fingerprint G≠F и у G нет своих записей — перенести записи F→G (мета,
членство, пометка «обработан»), залогировать; при конфликте (у G есть записи)
не трогать. Пока файл докачивается, записи просто переезжают по цепочке на
актуальный отпечаток при каждом скане — карантинов и таймеров нет намеренно.
Сознательный риск: замена контента по тому же пути наследует мету (правится
руками).

**Шаги реализации** (каждый отдельно, с проверкой):
1. ~~схема БД + API дерева/меты/членства (+ GC-эндпоинт), без правил~~ —
   СДЕЛАНО (2026-08-11): миграция 2 (Prism_Node/NodeItem/Meta, старая таблица
   удалена — была пуста), контракт `IMediaIdentity` в Abstractions (хост отдаёт
   fileKey), `fileKey` и мета подмешиваются в `/api/media`, эндпоинты
   `/api/library/*` + `PUT/DELETE /api/media/{id}/meta` (вместо `/metadata`).
   Проверено curl: группы/циклы/членство/мета/gc/каскад; мета пережила
   переименование файла;
2. ~~fingerprint-кэш, present-флаг, ремап~~ — СДЕЛАНО (2026-08-11), причём
   шире: **id файла теперь и есть fingerprint** (решение пользователя — убрать
   двойственность id/fileKey). Ядро: id = «размер-хеш» при скане, персистентный
   кэш отпечатков + «последний путь» в `data/fingerprints.json`, копии одного
   контента схлопываются в одну запись, `/api/resolve` — прямой поиск.
   `IMediaIdentity` сжат до `GetLiveIdsAsync`/`FindSuccessorAsync`. Плагин: мета
   без резолва (id = ключ), `present` в дереве, фоновый `LibraryRemapService`
   (при старте и раз в 5 мин). Проверено: id стабилен при переименовании, копия
   дедуплицируется, resolve работает, ремап перенёс мету+членство при подмене
   содержимого, HLS с длинными id играет;
3. правила автозаполнения;
4. минимальный UI дерева в веб-клиенте.

## 7. API для умного дома и плагин для ThinkingHome
- в умном доме (ThinkingHome) — **автообнаружение всех плееров Prism по MQTT**
  (подписка на `prism/player/+/info` и `prism/player/+/state` из контракта);
- из библиотеки — **включать кино на заданном плеере**: резолв «название → mediaId»
  через API Prism и публикация команды `open` в топик выбранного плеера.

Это сторона координатора (плагин ThinkingHome).

## 8. Сайт с документацией

## 9. Автозаполнение настроек плеера — в самом конце
Удобство, не необходимость; требует отдельного обсуждения. Детали — в
`Prism.Player.Android/TODO.md`, п.2 (QR / ссылка-приглашение / discovery /
дефолты от координатора).

## 10. Остальные MQTT-команды — пока НЕ делаем
`pause`/`resume`/`stop`/`seek`/`next`/`previous`/`setAudio`/`setSubtitle`/`setRate` —
нет уверенности, что они нужны. Вернёмся, если появится реальная потребность
(зависимость `setAudio`/`setSubtitle` от мультитрек-HLS снята — п.3 сделан).

---
Отдельный TODO по Android-плееру — в `Prism.Player.Android/TODO.md`.
